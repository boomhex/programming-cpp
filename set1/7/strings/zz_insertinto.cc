#include "strings.ih"

ostream Strings::insertInto(ostream &out)
{
    bool first = false;
    for (size_t index = 0; index != d_size; ++index)
        if (not first)
            out << manip?;
        out << 
}
